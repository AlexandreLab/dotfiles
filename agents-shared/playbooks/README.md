# Playbooks

Reusable, tool-agnostic playbooks referenced from `AGENTS.md`.

Each playbook is a plain markdown file describing a procedure any agent can follow:
- `<name>.md` — short, action-oriented, no tool-specific commands when avoidable.

Add a playbook here when you find yourself explaining the same procedure to multiple agents.
